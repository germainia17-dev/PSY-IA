{
  "framework": null,
  "buildCommand": "npx expo export --platform web --output-dir dist",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "trailingSlash": false,
  "installCommand": "npm install --legacy-peer-deps && npm install @expo/cli"
}
